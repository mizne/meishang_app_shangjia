import { Component, OnInit } from '@angular/core';
import {MatDialog, MatDialogRef, MAT_DIALOG_DATA} from '@angular/material';

@Component({
  selector: 'app-been-closed',
  templateUrl: './been-closed.page.html',
  styleUrls: ['./been-closed.page.scss'],
})
export class BeenClosedPage implements OnInit {

  constructor(public dialog: MatDialog) { }

  ngOnInit() {
  }
  toEdit(){
    
      }
}
